import { Layout } from 'layouts';
import React from 'react';
import { WordsTable, AddWord } from 'components';

const Home = () => {

	return (
		<Layout>
			<WordsTable />
		</Layout>
	);
};

export default Home;
